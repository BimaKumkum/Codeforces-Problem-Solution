#include<bits/stdc++.h>
using namespace std;
int main(){
int t,a,b,c;
cin>>t;
while(t--){
    cin>>a>>b>>c;
    int sum = a+b+c;
    int minValue = min(min(a,b),c);
    if(sum%9 == 0 && minValue>=(sum/9)){
        cout<<"Yes"<<endl;
    }
    else{
        cout<<"NO"<<endl;
    }
}
return 0;
}
